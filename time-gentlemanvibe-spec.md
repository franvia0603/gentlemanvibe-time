# time.gentlemanvibe.com — 프로젝트 스펙 문서

## 1. 프로젝트 개요

**컨셉**: 데스크테리어용 프리미엄 디지털/아날로그 클락 & 타이머 웹앱
**핵심 가치**: "Desk Wasting과 몰입을 동시에 만족시키는" 서브 모니터/태블릿 전용 시계
**도메인**: time.gentlemanvibe.com
**배포**: GitHub → Vercel (별도 프로젝트, 기존 gentlemanvibe.com과 분리)

---

## 2. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js 14+ (App Router) + TypeScript | Vercel과 최적 궁합, SSG 가능 |
| 스타일 | Tailwind CSS | 다크테마/톤온톤 팔레트를 디자인 토큰으로 관리 |
| 상태관리 | Zustand 또는 React Context | 클라이언트 전용, 백엔드 없음 |
| 저장소 | localStorage (설정) + IndexedDB/idb-keyval (프리셋·랩타임 다수 저장 시) | 회원가입 없는 로컬 지속성 |
| PWA | next-pwa + manifest.json + service worker | 오프라인 동작, 홈 화면 설치 |
| 시계 렌더링 | requestAnimationFrame + Page Visibility API 재동기화 | setInterval 드리프트 방지 |
| 아날로그 시계 | SVG + CSS transform | 글로우 효과, 커스텀 컬러 적용 용이 (Canvas보다 CSS 감성 표현에 유리) |
| 사운드 | Web Audio API | 정밀한 재생 제어, 볼륨 페이드 등 |

---

## 3. 브랜드 헤더 & 사이트 구조 (Phase 1 필수 포함)

### 3.1 상단 브랜딩 (헤더)
- 위치: 사이트 상단 정중앙 + **풀스크린/Zen 모드에서도 동일하게 상단 정중앙 유지**
- 구성: "GV" 로고 텍스트(상단) + 그 아래 "GentlemanVibe" 텍스트(하단), 2단 세로 배치
- **"GV"**: 엑스트라 볼드(굵게), 아래 "GentlemanVibe"보다 폰트 크기 더 크게 — 로고 역할을 하는 상단 요소로 존재감 있게 처리.
- **"GentlemanVibe"**: 대소문자 표기 그대로("GentlemanVibe", 이니셜만 대문자) 유지, 컬러는 화이트(또는 흰색에 가까운 밝은 톤)로 배경(매트 블랙)과 뚜렷이 대비되어 잘 보이도록 처리 — 기존의 낮은 투명도(워터마크 느낌)보다 선명도를 높인다.
- 기능: 클릭 시 `https://www.gentlemanvibe.com` 새 탭으로 이동하는 링크
- 톤: 로고로서 또렷하게 보이되, 시계보다는 작은 크기로 화면의 주인공을 침범하지 않는 선에서 배치.

### 3.2 하단 푸터
- **일반 사이트 모드 하단**: 카피라이트(`© 2026 GentlemanVibe. All rights reserved.` 형식) + About GentlemanVibe / Privacy & Policy / Contact Us 링크 (구석에 작게, 애드센스 심사 대비 필수 페이지)
- **풀스크린/Zen 모드 하단**: 카피라이트 최소 표기만 유지 (About/Privacy/Contact는 일반 모드에서만 노출 — Zen 모드는 몰입이 핵심이므로 페이지 이동 링크 최소화)
- About/Privacy Policy/Contact Us는 별도 정적 페이지(`/about`, `/privacy-policy`, `/contact`)로 구현

### 3.3 사이트 기본 테마
- **다크모드가 디폴트** (라이트모드 전환 옵션은 Phase 2 이후 검토, 1단계는 다크 고정 또는 최소 토글)

### 3.3.1 완전 몰입형 풀스크린 원칙 (수정 — 단일 요소만 노출)

풀스크린/Zen 모드의 목적은 "최대한 심플하고 화면이 고정되는 것"이다. 지금까지는 ModeNav만 숨기고 다른 요소(설정 패널, 브랜드 헤더 등)는 남아있는 경우가 있었는데, 이를 명확히 규정한다.

- **풀스크린 진입 시 화면에 남아야 하는 것은 딱 두 가지뿐이다**: (1) 현재 보고 있는 도구의 핵심 표시 요소, (2) 나가기(풀스크린 종료) 버튼.
  - Clock 모드 → 시계(디지털 또는 아날로그)만.
  - Focus(뽀모도로) 모드 → 다이얼과 남은 시간만.
  - Timer(카운트다운) 모드 → 다이얼과 남은 시간만.
  - World(월드클락) 모드 → 사용자가 선택/추가해둔 도시 카드들만 (도시 추가/삭제 버튼, 검색 UI 등 편집 기능은 숨김).
- **숨겨야 하는 것**: ModeNav, 설정(톱니바퀴) 패널 버튼, `FullscreenHint` 안내 문구, 하단 푸터(카피라이트/About/Privacy/Contact 링크), 그 외 옵션성 컨트롤 전부.
- **브랜드 로고(GV)는 계속 노출**하되, 아래 3.3.2의 안전 영역 규칙을 따른다.
- **나가기 버튼은 반드시 유지**: 모바일에서는 ESC 키를 쓸 수 없으므로, 풀스크린 진입 후에도 화면 안에 풀스크린 종료(나가기) 버튼이 항상 보이고 클릭 가능해야 한다. (기존 `FullscreenToggle`이 이 역할을 겸함 — 진입 전엔 "시작" 버튼, 진입 후엔 "나가기" 버튼으로 상태에 따라 아이콘만 바뀌는 동일 버튼.)

### 3.3.2 노치·카메라 컷아웃 안전 영역(Safe Area) 대응

모바일 기기(특히 삼성 등 펀치홀/카메라 컷아웃이 화면 상단 중앙에 있는 기종)에서 풀스크린 진입 시, 화면 정중앙 상단에 있는 GV 로고가 카메라 구멍과 정확히 겹쳐 로고가 잘리는 문제가 확인됐다.

- 풀스크린 상태의 로고(및 나가기 버튼 등 상단에 남는 요소)는 CSS `env(safe-area-inset-top)`을 이용해 노치/카메라 컷아웃 영역만큼 아래로 충분히 밀어내려 배치한다.
- `viewport-fit=cover`가 meta 태그에 설정되어 있어야 `env(safe-area-inset-*)` 값이 정상적으로 계산된다 — PWA manifest/meta 설정 시 함께 점검한다.

### 3.4 모드 진입 안내 문구 (풀스크린 유도 UX)
- **목적**: 일반(비풀스크린) 화면에서 방문자가 "이 사이트는 전체화면으로 써야 진짜 가치가 나온다"는 걸 바로 알게 한다.
- **위치**: 각 기능(클락/월드클락/타이머/알람/스톱워치/뽀모도로) 화면의 하단 또는 여백에, 튀지 않는 작은 안내 문구로 배치.
- **톤 & 카피 원칙**: "무료 [기능명] 사용법: 탭이나 폰에서 전체화면 버튼을 누르시면 깔끔한 GV [기능명] 모드로 나만의 데스크테리어를 완성시켜드립니다." 형태를 기본 템플릿으로 하되, 기능명만 교체.
  - 예시(클락): "무료 디지털 클락 사용법: 탭이나 폰에서 전체화면 버튼을 누르시면 깔끔한 GV Clock 모드로 나만의 데스크테리어를 완성시켜드립니다."
  - 예시(뽀모도로): "무료 뽀모도로 타이머 사용법: 탭이나 폰에서 전체화면 버튼을 누르시면 깔끔한 GV Focus 모드로 나만의 데스크테리어를 완성시켜드립니다."
  - 예시(스톱워치): "...GV Stopwatch 모드로..."
  - 예시(월드클락): "...GV World Clock 모드로..."
  - 예시(알람): "...GV Alarm 모드로..."
- **전용 풀스크린 토글(확정 — 위치/스타일 통일)**: 하단 안내 문구를 클릭해야만 풀스크린으로 갈 수 있는 방식은 발견성이 낮다. **모든 도구 페이지(Clock/Focus/Stopwatch/Timer/World)에서 동일한 위치와 스타일**로 풀스크린 토글 버튼을 노출한다.
  - **위치**: `SiteHeader`의 네비게이션 메뉴(ModeNav, Clock/Focus/Stopwatch/Timer/World 탭) **좌측**에 통일 배치. 페이지마다 다이얼 옆, 시계 아래 등 제각각이던 기존 방식은 폐기하고, 헤더 한 곳에서만 관리한다.
  - **스타일**: 오렌지(앰버 계열) **배경을 채운 버튼**으로 처리 — 이 버튼만은 5.0.1의 "테두리 대신 텍스트로 강조" 원칙의 예외로, 사이트에서 가장 자주 쓰이는 핵심 액션이므로 배경색으로 확실한 존재감을 준다. 아이콘(확장 화살표 등)은 배경과 대비되는 어두운 색(`gv-matte-black` 또는 `gv-charcoal`)으로 표시해 아이콘 자체의 시인성도 확보한다.
  - 풀스크린 중에도 계속 노출되어(다른 헤더 요소는 숨겨져도) 언제든 빠져나올 수 있어야 한다.
- `FullscreenHint` 문구는 보조 안내로 하단에 계속 유지하되, 유일한 진입 경로가 되어서는 안 된다.
- 재사용 가능한 공통 컴포넌트(`FullscreenHint`, `FullscreenToggle`)로 구현해서, 기능이 추가될 때마다 자동으로 동일하게 적용되게 한다.

### 3.5 메인 클락 표시 옵션 (Display Settings)

디지털/아날로그 클락 화면에 설정 패널(또는 간단한 토글 메뉴)을 두고, 아래 항목을 개별 on/off 할 수 있게 한다.

- **오전/오후(AM/PM) 표시**: 12시간제 사용 시 AM/PM 라벨 표시 여부. 끄면 24시간제로 표시.
- **시간만 표시**: 초 단위를 숨기고 시:분만 크게 표시하는 모드 (더 미니멀한 데스크테리어 연출용).
- **날짜·요일 표시**: 클락 아래/위에 날짜와 요일을 작게 병기.
- **날씨 표시(위치 기반)**: 브라우저 Geolocation API로 현재 위치를 얻어 실시간 날씨(기온·아이콘 정도의 최소 정보)를 클락 옆 또는 아래에 작게 표시. 위치 권한 거부 시에는 조용히 숨김 처리(에러 노출 없이).
  - 날씨 API는 무료/저비용 티어가 있는 서비스(예: Open-Meteo — API 키 불필요, 무료) 사용을 우선 검토.
- **저장**: 각 옵션은 localStorage에 저장되어 다음 방문 시에도 유지 (섹션 3.3/스펙 2번 저장소 정책과 동일 패턴).
- **UI**: 설정 아이콘(예: 톱니바퀴) 클릭 시 작은 패널/드롭다운으로 토글 노출. 풀스크린 Zen 모드에서는 숨김(다른 UI 요소와 동일 원칙).

### 3.6 도구 페이지 콘텐츠 & 광고 자리 (참고: vClock.kr 구조 분석)

경쟁 유틸리티 사이트(vClock.kr)의 실제 소스 구조를 분석한 결과를 반영한다.

- **사용법 설명 섹션**: 각 도구 화면(클락/타이머/알람/스톱워치/뽀모도로) 아래에 `<h1>[도구명] 사용법</h1>` + 300~500자 내외의 설명 문단을 배치. 이 섹션이 페이지의 SEO 핵심 콘텐츠 역할을 한다.
  - 예: "GV Clock 사용법", "GV Focus(뽀모도로) 사용법" 등 GentlemanVibe식 네이밍 유지.
- **광고 삽입 자리(Placeholder)**: 도구 패널과 사용법 설명 섹션 사이에, 지금 단계부터 광고 슬롯 자리(빈 컨테이너)를 미리 확보해둔다. 실제 애드센스 코드는 승인 후 수동 삽입(섹션 7 참고).
- **하단 정책 링크**: 이미 섹션 3.2에 정의된 About/Privacy/Contact 구조를 그대로 유지 — vClock의 연락처/약관/개인정보 링크 패턴과 동일한 역할.
- **채택하지 않는 패턴**: vClock의 공유 버튼, embed 소스코드 기능, 대량의 SEO 랜딩 페이지(`/타이머-25-분/` 등 개별 URL 양산)는 GentlemanVibe 브랜드 정체성과 맞지 않아 기본적으로 채택하지 않는다. 다만 뽀모도로 25/5분처럼 검색 수요가 뚜렷한 극소수 조합에 한해 추후(Phase 3 이후) 선별 적용을 검토할 수 있다.

### 3.7 "시간이야기" 카테고리 (콘텐츠 허브, 신규)

애드센스 콘텐츠 요건 보강 및 자연스러운 내부링크 구조를 위해, 시간 관리·시간의 과학/역사를 다루는 정보 글 모음을 별도 카테고리로 신설한다.

- **네비게이션**: `SiteHeader`의 ModeNav에 여섯 번째 탭으로 "시간이야기" 추가, 라우트는 `/guide`. 기존 5개 탭(Clock/Focus/Stopwatch/Timer/World)과 동일한 위치·스타일 규칙(5.0.1, 5.0.3)을 따른다 — 탭이 하나 늘어난 만큼 좁은 뷰포트에서 줄바꿈이 발생해도 3.3.1/3.4에서 이미 해결한 동적 헤더 높이 계산(`--gv-header-height`) 덕분에 본문 잘림은 발생하지 않아야 한다.
- **목록 페이지(`/guide`)**: 20개 글 전체를 카드 그리드로 나열. 카테고리 2개(시간 관리 이론·기법 / 시간의 과학·역사)로 구분해 보여준다.
- **개별 글 페이지(`/guide/[slug]`)**: 각 글은 독립된 라우트를 가진다. StaticPageShell과 유사한 읽기용 레이아웃(좌측 정렬 본문) 재사용을 검토.
- **도구 페이지 하단 랜덤 노출(신규 컴포넌트, 가칭 `TimeStoriesWidget`)**: Clock/Focus/Stopwatch/Timer/World 각 페이지의 `UsageGuide`(또는 조리팁) 섹션과 푸터 사이에, 20개 글 중 **무작위로 2~3개**를 카드/링크 형태로 노출한다.
  - 방문(또는 페이지 로드)마다 다른 조합이 나오도록 클라이언트에서 랜덤 선택.
  - "더 많은 시간 이야기 보기" 같은 문구로 `/guide` 목록 페이지 전체 링크도 함께 제공.
  - 풀스크린 모드에서는 다른 부가 콘텐츠와 마찬가지로 숨김(3.3.1 원칙).
  - 디자인은 사이트 팔레트(매트 블랙/차콜 카드, gv-titanium 텍스트, hover 시 gv-amber) 기준.
- **작성 방식**: 한 번에 20개를 전부 작성하지 않고, 5개씩 나눠 순차적으로 작성·검토 후 반영한다.
- **RSS 피드**: `/guide` 글 목록에 대한 RSS 피드(`/guide/rss.xml` 또는 `/rss.xml`)를 제공한다. 도구 페이지(Clock/Focus 등)는 콘텐츠가 아니라 기능이므로 RSS 대상에서 제외 — "시간이야기" 글에만 적용. 네이버 서치어드바이저 등록 시 RSS 제출 항목과 연동.

---

## 4. 단계별 기능 범위 (Phased Rollout)

### Phase 1 — 코어 + 브랜드 감성 (동시 진행)
- [ ] 디지털 클락 (초저지연, 정확한 로컬 시간)
- [ ] 아날로그 클락 (SVG 기반, 디지털과 토글 전환)
- [ ] 플립 클락 애니메이션 (이징 적용, 기계적이지 않은 물리감)
- [ ] 뽀모도로 타이머 (25/5분 기본값 + 커스텀 가능)
- [ ] 커스텀 카운트다운 타이머 (뽀모도로와 별개 기능, 섹션 5.2 참고)
- [ ] 스톱워치 + 랩타임 (밀리초 단위)
- [ ] 다크 테마 디폴트 적용 (매트 블랙/차콜 베이스)
- [ ] 톤온톤 컬러 팔레트: 매트 블랙, 티타늄 그레이, 포레스트 그린, 웜 베이지, 앰버 글로우, True Black(OLED)
- [ ] 풀스크린 Zen 모드 (Fullscreen API)
- [ ] 상단 중앙 GV/GentlemanVibe 브랜드 헤더 + 메인 사이트 링크 (섹션 3.1)
- [ ] 하단 푸터: 카피라이트 + About/Privacy Policy/Contact Us 링크 (섹션 3.2)
- [ ] About / Privacy Policy / Contact Us 정적 페이지 3종
- [ ] 모드 진입 안내 문구 컴포넌트(`FullscreenHint`, 섹션 3.4) — 클락/타이머/스톱워치/뽀모도로 등 각 화면에 공통 적용
- [ ] 뽀모도로 타이머 UI를 원형 다이얼+세그먼트 링 방식으로 구현 (섹션 5.1)
- [ ] 메인 클락 표시 옵션 패널: AM/PM, 시간만 표시, 날짜·요일, 날씨(위치 기반) (섹션 3.5)
- [ ] 도구 페이지별 사용법 설명 섹션 + 광고 삽입 자리 확보 (섹션 3.6)
- [ ] localStorage 기반 설정 자동 저장 (마지막 테마, 마지막 타이머 값)
- [ ] PWA 기본 설정 (manifest, 아이콘, 홈 화면 설치)
- [ ] 반응형 레이아웃 (서브 모니터 가로형 최적화)
- [ ] 기본 SEO 세팅 (섹션 7 참고): 메타태그, sitemap.xml, robots.txt, 구조화 데이터

### Phase 2 — 사운드 & 확장 유틸리티
- [ ] 알람/타이머 종료음 프리셋 (시그니처 사운드 큐레이션/생성, 섹션 5 참고)
- [ ] 백색소음 재생 (비, 로파이, 카페 앰비언스)
- [ ] 월드 클락 (카드형 그리드, 도시 추가/삭제, 섹션 6 참고)
- [ ] 프리셋 다중 저장 (IndexedDB 전환)

### Phase 3 — 브랜드 연계 & 폴리시
- [ ] 시간대별(아침/오후/밤) 배경 밝기·무드톤 자동 미세 변화
- [ ] GentlemanVibe 메인 사이트 연계 링크 (하단 미니멀 텍스트)
- [ ] 성능/접근성 최종 점검 (Lighthouse 90+ 목표)

---

## 5. 디자인 방향

**한 줄 정의**: 클래식하고 심플하며, 기계적이지 않은 모던 미니멀

- **타이포그래피**: Inter / Manrope 등 휴머니스트 산세리프, 얇은 굵기 + 넓은 트래킹의 tabular-nums. 세븐세그먼트/디지털 계산기 폰트 지양. 라벨 텍스트에 세리프 소량 배합 가능 (클래식함 보강).
- **컬러 팔레트**: 매트 블랙·차콜 배경 + 앰버/웜 아이보리 글로우. 네온 시안·라임그린 등 전형적 "테크" 컬러 배제.
- **브랜드 헤더 컬러**: 백색에 가까운 미색(`#F5F1E8` 계열, warm off-white) — 시계 본체의 앰버 글로우와 톤을 맞추되 구분되도록 채도 낮은 미색 유지.
- **모션**: 플립/전환 애니메이션에 자연스러운 이징 적용 — 뚝뚝 끊기는 디지털 전환이 아닌 아날로그 기계식 물리감.
- **레이아웃**: 여백을 넉넉히 두는 미니멀 그리드. 불필요한 장식·구분선 없이 타이포와 여백만으로 위계 표현.
- **숫자 폰트(확정)**: 클락/타이머의 시간 숫자는 **Bebas Neue**(빈티지 플립 클락/공항·기차역 분할판 시계 느낌, 두꺼운 획) 사용. 브랜드 헤더 및 일반 UI 텍스트는 계속 Inter 유지.

### 5.0.1 UI 컨트롤(버튼/토글/네비게이션) 스타일 원칙 — 시각적 위계 수정

실제 구현 화면 검토 중 발견된 문제: 버튼/입력 필드의 **테두리(border)가 흰색 등 밝은 톤으로 강하게 처리되어, 내부 텍스트보다 테두리가 시각적으로 먼저 눈에 들어오는 역전 현상**이 발생했다. 이는 "글자를 읽는" 사이트 목적과 어긋나므로 아래 원칙으로 교정한다.

- **주인공은 항상 텍스트/숫자**: 버튼이든 네비게이션이든, 테두리·배경보다 안의 텍스트가 시각적으로 더 도드라져야 한다.
- **테두리는 톤을 낮춘다**: 흰색·밝은 오프화이트 등 고대비 테두리 금지. `gv-titanium`을 낮은 불투명도(예: 30~40%)로 쓰거나, 아예 테두리 없이 은은한 배경 채움(`gv-charcoal` 등)으로 대체하는 것도 고려.
- **텍스트 크기·명도를 충분히 확보**: 네비게이션 라벨(Clock/Focus), 버튼 텍스트(시작/리셋/모드 전환), 스텝퍼(+/-) 등 모든 인터랙션 요소의 글자가 배경 대비 충분히 밝고, 최소 판독 가능한 크기 이상이어야 한다. 작다고 느껴지면 폰트 크기를 올린다.
- **일관된 강조 규칙**: 활성/포커스 상태는 `gv-amber` 계열 텍스트 색상 변화로 표현하고, 테두리를 굵게 하거나 밝게 하는 방식으로 강조하지 않는다.
- **보조/안내 텍스트 대비 기준(추가)**: `FullscreenHint` 문구, 하단 카피라이트, 사용법 안내문 등 낮은 불투명도로 처리하는 "보조 텍스트"도 매트 블랙 배경 위에서 **읽는 데 지장 없는 최소 대비**를 반드시 유지한다. 무조건 톤을 낮추기보다, 실제로 읽어보고 판단한다 — 불투명도를 과도하게 낮추거나(예: /30 이하) 너무 어두운 색상(`gv-titanium`의 저채도 톤 등)을 본문 텍스트에 그대로 쓰면 다크 배경에서 거의 안 보이는 문제가 생긴다. 폰트 굵기(font-weight)가 너무 얇아도(예: font-thin) 작은 크기와 겹치면 흐릿해 보이므로, 안내/보조 텍스트는 최소 `font-normal` 이상 + 충분한 밝기(`gv-titanium` 원색 또는 `gv-brand-offwhite`)를 기본값으로 삼는다.

### 5.0.2 완전 반응형 원칙

- 사이트의 모든 화면(클락/뽀모도로/향후 추가되는 모든 도구 페이지, 설정 패널, 버튼, 텍스트 포함)은 모바일/태블릿/데스크톱/서브 모니터 등 **모든 뷰포트 크기에서 깨짐 없이 완전히 반응형**으로 동작해야 한다.
- 시계 숫자·다이얼 같은 핵심 요소뿐 아니라, 네비게이션 라벨·버튼·설정 패널 텍스트 등 **모든 UI 텍스트 요소도 뷰포트에 비례해 적절히 스케일**되어야 하며, 특정 해상도에서만 다른 요소와 겹치거나 잘리는 현상이 없어야 한다.
- **페이지별 콘텐츠 양에 맞는 여백 관리**: `PageShell`의 상하단 여백은 고정값이 아니라, 각 페이지가 실제로 담고 있는 콘텐츠 양(제목 입력, 다이얼, 스피너, 프리셋 버튼, 안내문 등)을 기준으로 충분히 확보한다. 콘텐츠가 많은 페이지(예: 타이머)에서 상단 네비게이션/제목 입력이 겹치거나 하단 버튼/안내문이 겹치는 문제가 반복되지 않도록, 새 페이지를 추가할 때마다 반드시 실제 콘텐츠 높이 기준으로 여백을 재점검한다.

### 5.0.3 모바일 우선(Mobile-First) 설계 원칙 — 최우선 순위

지금까지 반복된 겹침·잘림 버그의 근본 원인은 **데스크톱 레이아웃을 먼저 만들고 모바일을 나중에 욱여넣는 방식**이었다. 이 순서를 뒤집는다.

- **설계 순서 자체를 모바일부터**: 새 화면/컴포넌트를 만들 때 데스크톱 기준으로 먼저 배치하고 미디어쿼리로 축소하는 방식이 아니라, **가장 좁은 모바일 뷰포트(375px 기준)에서 모든 요소가 겹침·잘림 없이 완결된 레이아웃을 먼저 완성**한 뒤, 태블릿·데스크톱으로 점진적으로 확장(progressive enhancement)한다.
- **터치 타깃 크기**: 버튼·아이콘·스텝퍼 등 클릭 가능한 요소는 모바일에서 손가락으로 누르기 충분한 크기(최소 40~44px 권장)를 기본값으로 삼는다.
- **세로 스크롤 우선**: 가로 스크롤이나 요소 잘림이 생기느니, 모바일에서는 세로로 자연스럽게 쌓이는(stack) 레이아웃을 기본으로 한다. (예: 타이머의 시/분/초 스피너처럼 가로 배치가 좁은 화면에서 깨지는 요소는 세로 스택 등으로 먼저 모바일에서 자연스러운 형태를 정하고, 그 다음 넓은 화면에서 가로 배치로 확장하는 순서로 설계.)
- **프리셋/그리드형 버튼 묶음**(라면 프리셋, 월드클락 카드 등)은 모바일에서 1~2열, 태블릿 2~3열, 데스크톱 3~5열처럼 열 수 자체가 뷰포트에 따라 달라지는 반응형 그리드로 설계하며, 특정 뷰포트에서 버튼이 화면 밖으로 밀려나거나 글자가 잘리는 일이 없어야 한다.
- **이후 모든 신규 기능 개발 시 기본 규칙**: 새 컴포넌트를 만들 때는 먼저 모바일 뷰포트로 스크린샷을 찍어 확인한 뒤, 태블릿·데스크톱 확인으로 넘어가는 순서를 따른다 (지금까지의 반대 순서를 뒤집음).

### 5.1 뽀모도로 타이머 UI 방향 (수정본)

물리적 원형 다이얼 타임 타이머(첨부 참고 이미지 — 무소음 스터디 타이머류)를 웹 UI로 재해석한다.

- **원형 다이얼**: 시계 문자반처럼 원 둘레에 0/5/10/15.../55 분 눈금과 작은 숫자 라벨을 표시.
- **세그먼트 링(Segment Ring) — 방향 중요**: 시간을 설정하면(예: 25분) **처음부터 해당 분량만큼 세그먼트가 꽉 채워진 상태**로 시작한다. 시간이 흐를수록 **채워진 세그먼트가 줄어들며(비활성화되며) 남은 시간만큼만 채워진 상태로 유지**된다 — 즉 "경과한 만큼 채워지는" 방식이 아니라 **"남은 시간만큼 채워져 있다가 줄어드는"** 방식이다. (이전 구현은 방향이 반대였음 — 반드시 역전해서 수정.)
- **중앙 디지털 표시**: 원 중심에 Bebas Neue 폰트로 남은 시간(MM:SS) 표시.
- **배경**: 매트 블랙(참고 이미지의 물리적 베젤·버튼·배터리 아이콘 같은 하드웨어 디테일은 재현하지 않음).
- **시간 설정 방법 (세 가지 모두 지원)**:
  1. 프리셋 버튼 (25분 집중 / 5분 휴식 등 기존 방식 유지)
  2. **직접 숫자 입력**: 분(그리고 필요시 시) 값을 직접 타이핑해서 설정할 수 있는 입력 필드 제공.
  3. **다이얼 클릭/드래그로 설정 — 1:1 정밀 매핑(수정)**: 다이얼 둘레를 클릭하거나 드래그하면 그 지점에 해당하는 시간으로 즉시 설정된다. 지금까지는 클릭한 지점이 특정 눈금 단위(예: 5분 단위)로 스냅되어, 사용자가 실제로 짚은 위치와 채워지는 세그먼트 링의 끝 지점이 시각적으로 어긋나 보이는 문제가 있었다. **클릭/터치한 각도를 0~60 눈금 전체 범위에 정확히 1:1로 매핑**해서, 사용자가 짚은 정확한 위치까지 세그먼트가 채워지도록 한다 — 손가락(또는 커서)이 놓인 자리와 그래프가 끝나는 자리가 항상 일치해야 한다.
     - **실제 원인 확인(중요)**: 분→각도 계산 로직 자체는 정확했다. 문제는 **세그먼트 0번(시작점)이 렌더링되는 실제 회전 위치가 다이얼의 "0" 눈금(12시 방향)이 아니라 "45" 눈금 위치부터 그려지고 있었던 회전 오프셋 버그**였다. 즉 값 계산과 시각적 렌더링 사이에 회전 기준점이 어긋나 있었던 것 — 세그먼트 0번은 반드시 다이얼 상단 "0" 라벨과 정확히 같은 각도(12시 방향, 0°)에서 시작하도록 SVG 회전 기준을 재보정해야 한다.
- **색상 (첨부 이미지 기준으로 확정)**:
  - 세그먼트(남은 시간 표시): 레드/오렌지 톤 (`gv-amber`보다 살짜 더 붉은 톤 — 참고 이미지의 채도 높은 오렌지-레드 계열을 기준으로 별도 토큰 검토, 예: `gv-timer-red` 추가 가능)
  - 중앙 숫자 및 다이얼 눈금 라벨: 화이트/오프화이트 (참고 이미지처럼 선명하게)
  - 비활성(줄어든 뒤 빈) 세그먼트: `gv-charcoal` 같은 어두운 톤 유지

### 5.2 커스텀 카운트다운 타이머 UI 방향 (뽀모도로와 별개 기능)

**뽀모도로(Focus)와 명확히 다른 목적의 기능**이다. 뽀모도로는 집중↔휴식 사이클용, 이 타이머는 "라면 끓이기 3분", "회의 30분"처럼 **한 가지 작업에 필요한 단발성 시간**을 재는 범용 타이머다. 참고: vClock.kr의 타이머 수정 모달 구조.

- **입력 방식**: 시/분/초를 각각 스피너(< 00 >) 또는 직접 타이핑으로 설정.
- **프리셋 = 라면 브랜드 조리시간 (확정)**: 범용 "1분/3분/5분" 버튼 대신, GentlemanVibe 톤에 맞는 재미 요소로 대표 라면 브랜드별 정확한 조리시간을 프리셋 버튼으로 제공한다.

  | 라면 | 제조사 | 조리시간 | 비고 |
  |---|---|---|---|
  | 신라면 | 농심 | 4분 30초 | 물 550ml |
  | 너구리 | 농심 | 5분 | 물 550ml, 면과 스프를 같이 넣고 끓임 |
  | 안성탕면 | 농심 | 4분 30초 | 물 550ml |
  | 짜파게티 | 농심 | 5분 | 물 600ml로 삶은 뒤 물 8스푼 남기고 비비기 |
  | 진라면(순한맛/매운맛) | 오뚜기 | 4분 | 물 550ml |
  | 참깨라면 | 오뚜기 | 4분 | 물 500ml, 계란블록·유성스프는 조리 후 마지막에 |
  | 진짬뽕 | 오뚜기 | 5분 | 물 500ml |
  | 불닭볶음면 | 삼양식품 | 5분 | 물 600ml로 삶은 뒤 물 8스푼 남기고 볶기 |
  | 삼양라면 | 삼양식품 | 4분 | 물 550ml |
  | 팔도비빔면 | 팔도 | 3분 | 끓인 후 찬물에 헹궈 비비기 |

  - 버튼 라벨: "신라면 4분 30초"처럼 이름+시간을 함께 표시.
  - 클릭 시 해당 시간으로 다이얼이 즉시 채워지고 설정된다.
  - **조리 안내 문구(하단, 섹션 3.6 사용법 설명 섹션과 결합)**: 프리셋 표 아래(또는 사용법 설명 섹션 자리)에 "조리 팁: 봉지 뒷면에 적힌 정량의 물과 조리시간을 지키는 것이 가장 맛있게 끓이는 비결입니다." 같은 짧은 팁 문구를 포함한다. 이 안내문은 5.0.1에서 정의한 "보조 텍스트 대비 기준"을 반드시 지켜 다크 배경에서도 또렷하게 보이도록 한다.
- **종료 시 동작 선택**: 타이머 정지 / 타이머 재시작(반복) / 계속(0 이후 경과 시간으로 계속 증가) 세 가지 옵션 제공.
- **종료음 선택**: 브랜드 시그니처 사운드 프리셋(섹션 7 참고) 중에서 드롭다운으로 선택. 재생 미리듣기 버튼 포함. 반복 재생 여부 토글. (Phase 1 단계에서는 플레이스홀더 비프음으로 임시 구현 후, Phase 2에서 실제 사운드로 교체)
- **제목(선택)**: 종료 시 이 제목이 알림 메시지에 함께 표시됨.
- **테스트 버튼**: 실제 시작 전에 종료음/볼륨을 미리 들어볼 수 있는 테스트 기능.
- **비주얼**: 뽀모도로처럼 원형 다이얼+세그먼트 방식(`SegmentDial` 공통 컴포넌트)을 재사용하되, 색상은 뽀모도로의 레드-오렌지(`gv-timer-red`)와 구분되는 `gv-amber` 계열로 시각적으로 구별한다.
- **저장**: 최근 사용한 프리셋을 localStorage에 몇 개 기억.
- **화면 레이아웃 순서(확정)**: 실제 사용 화면 검토 후 아래 순서로 재배치한다 — 시간 설정과 시작 버튼이 서로 가깝게 붙어 있어야 조작 흐름이 자연스럽다.
  1. 다이얼(SegmentDial) + 중앙 남은 시간 표시
  2. 시/분/초 스피너 (다이얼 바로 아래)
  3. **시작/리셋(정지) 버튼** — 시간 설정 바로 아래, 라면 프리셋보다 먼저 오도록 배치 (지금까지는 맨 아래에 있었는데, 시간 설정과 붙여서 조작 동선을 짧게 한다)
  4. 라면 조리시간 프리셋 그리드
  5. 조리 팁 안내문
  6. 종료 시 동작 선택(정지/재시작/계속), 종료음 테스트 — 부가 설정이므로 프리셋보다 아래로
  7. 풀스크린 토글, FullscreenHint (다른 페이지와 동일하게 맨 아래)
- **제목(선택) 입력 필드 — 제거**: 상단에 있던 "제목 (선택, 예: 라면)" 입력 필드는 실제 화면에서 별다른 역할 없이 상단 네비게이션과 겹치는 문제만 일으켰다. **완전히 제거**한다. (종료 알림에 제목을 표시하는 기능 자체가 꼭 필요하면 추후 별도 UX로 재검토하되, 기본 화면 상단에 상시 노출하지 않는다.)

---

## 6. 월드 클락 (Phase 2)

**참고**: vClock.kr의 카드형 그리드 구조를 기반으로 하되, GentlemanVibe 다크·미니멀 톤으로 재해석한다.

- **레이아웃**: 도시별 카드를 그리드로 배치. 각 카드에 도시명, 현재 시각(해당 도시 로컬 타임), 기준 도시 대비 시차("오늘, +9시간" 등) 표시.
- **기준 도시 카드 표기(수정)**: 최상단 기준 도시 카드에 단순히 "기준 도시"라는 라벨만 표시하지 않고, **실제 도시명("서울") 또는 "현재 위치"로 명확히 표시**한다. 방문자가 이게 어느 도시를 기준으로 삼은 건지 바로 알 수 있어야 한다.
- **기본 도시 목록 (최종 확정, 9개 → 8개)**: 서울(기준), 뉴욕, 런던, 파리, 시드니, 두바이, 로스앤젤레스, 토론토. **도쿄는 제외** — 서울과 시차가 없어(KST=JST) 비교 정보로서 의미가 없다.
- **도시 추가**: "추가" 버튼으로 국가/도시를 검색해서 새 카드를 추가할 수 있어야 한다. 국가를 선택하면 해당 국가의 대표 시간대가 반영되는 방식(vClock 참고 이미지와 동일한 UX).
- **삭제/편집**: 각 카드에 케밥 메뉴(⋮) 등으로 삭제 가능.
- **디자인 톤**: vClock의 밝은 화이트 카드 대신, GentlemanVibe의 매트 블랙/차콜 카드 + Bebas Neue 숫자 + 앰버 포인트로 재해석. 시차 표시 등 보조 텍스트는 `gv-titanium`.
- **저장**: 추가한 도시 목록은 localStorage(또는 스펙 2번의 IndexedDB 전환 정책)에 저장해 다음 방문 시 유지.
- **상단 카드 잘림 방지(중요)**: `SiteHeader`(로고+ModeNav+풀스크린 토글)는 뷰포트 폭에 따라 한 줄/두 줄로 높이가 달라질 수 있다(예: 좁은 화면에서 탭이 줄바꿈되면 헤더가 더 높아짐). 이런 가변 헤더 높이 때문에 본문 최상단 요소(월드 클락의 기준 도시 카드 등)가 헤더에 가려 잘리는 문제가 실제로 발생했다. 고정 `pt-*` 값 하나로 모든 뷰포트를 커버하려 하지 말고, **헤더의 실제 렌더링 높이를 기준으로 본문 상단 여백을 동적으로 계산**하거나, 헤더가 줄바꿈되는 임계 뷰포트에서도 절대 겹치지 않을 만큼 충분히 넉넉한 안전 여백을 둔다.

---

## 7. 사운드 방향

**한 줄 정의**: 일반적인 알림음이 아닌, 은은하고 특별한 브랜드 시그니처 사운드

- **알람/종료음**: AI 사운드 생성 도구(예: ElevenLabs Sound Effects)로 "부드러운 아날로그 벨, 낮은 리버브, 서두르지 않는 톤" 등 구체적 프롬프트를 줘서 브랜드 전용 사운드로 신규 제작. 기성 무료 라이브러리의 전형적 "띵동/삐삐" 사운드는 배제.
- **백색소음**: Freesound.org의 CC0 라이선스 필드 레코딩 중 후보를 선별해 별도로 청취·선택하는 프로세스로 진행 (구현 단계에서 별도 세션으로 진행 예정).
- **재생 방식**: Web Audio API로 페이드인/아웃 적용, 볼륨 조절 가능하게 구현.

---

## 8. SEO 최적화 & 애드센스 대비 (Phase 1 필수)

### 8.1 온페이지 SEO
- [ ] `<title>`, `<meta description>` 페이지별 최적화 (홈/About/Privacy/Contact 개별 설정)
- [ ] Open Graph + Twitter Card 메타태그 (SNS 공유 시 미리보기 이미지·설명 노출)
- [ ] `robots.txt` 작성 (크롤링 허용 범위 명시)
- [ ] `sitemap.xml` 자동 생성 (Next.js `sitemap.ts` 활용, 페이지 추가 시 자동 반영)
- [ ] 구조화 데이터(JSON-LD) — WebApplication 또는 SoftwareApplication 스키마 적용
- [ ] 시맨틱 HTML 구조 (h1/h2 위계, `alt` 텍스트, 접근성 랜드마크)
- [ ] canonical URL 설정
- [ ] favicon, PWA 아이콘 세트, `manifest.json`에 name/description 명시 (PWA 항목과 연동)
- [ ] Core Web Vitals 최적화 (이미지 최적화, 폰트 preload, 불필요한 JS 최소화)

### 8.2 필수 정적 페이지 (애드센스 심사 대비)
- `/about` — GentlemanVibe 브랜드 및 서비스 소개
- `/privacy-policy` — 개인정보처리방침 (쿠키/로컬스토리지 사용 고지 포함)
- `/contact` — 문의 채널 (이메일 또는 폼)

### 8.3 프로젝트 완료 후 제출 항목 (Google Search Console)
- [ ] `sitemap.xml` 최종 URL을 GSC에 제출
- [ ] GSC 소유권 확인 (DNS TXT 레코드 또는 HTML 메타태그 방식 중 선택)
- [ ] `robots.txt`에 sitemap 위치 명시 (`Sitemap: https://time.gentlemanvibe.com/sitemap.xml`)
- [ ] 색인 요청(URL 검사 도구)으로 주요 페이지 수동 색인 요청
- [ ] 향후 애드센스 신청 전 About/Privacy/Contact 페이지 실제 콘텐츠 채움 여부 최종 점검

---

## 9. 배포 절차 (Vercel + GitHub + 서브도메인)

1. GitHub에 새 리포지토리 생성 (예: `gentlemanvibe-time`)
2. Vercel에서 해당 리포지토리를 새 프로젝트로 Import
3. Vercel 프로젝트 설정 → Domains → `time.gentlemanvibe.com` 추가
4. Vercel이 안내하는 CNAME 레코드 값을 기존 `gentlemanvibe.com`을 관리 중인 DNS(가비아/카페24 등)에 등록
5. DNS 전파 확인 후 Vercel에서 도메인 활성화 확인
6. 이후 GitHub main 브랜치 push 시 자동 배포(CI/CD) 확인
7. 배포 완료 후 섹션 8.3의 GSC 제출 절차 진행

---

## 10. 미해결/추후 결정 사항

- 백색소음 최종 음원 선정 (구현 단계에서 후보 청취 후 결정)
- 시간대별 무드톤 자동 변화의 구체적 색상 곡선
- GSC 소유권 확인 방식(DNS TXT vs HTML 태그) 결정

**결정 완료 (기록용)**
- 월드 클락 기본 도시: 서울(기준), 뉴욕, 런던, 파리, 시드니, 두바이, 로스앤젤레스, 토론토 (8개, 도쿄는 서울과 시차 없어 제외)
- Contact 페이지 연락 채널: novamedia101@gmail.com (폼 없이 이메일만)
